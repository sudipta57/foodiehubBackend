{
    "version": 2,
    "name": "foodiehubBackend",
    "builds": [
       { "src": "app.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "/app.js" }
    ],
    "rewrites": [
      {
        "src": "/uploads/(.*)",
        "dest": "/uploads/$1"
      }
    ]
 }